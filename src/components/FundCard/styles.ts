import styled from 'styled-components/native';
import {Colors} from '../../resources';

export const FundContainerPress = styled.TouchableOpacity`
  width: 140px;
  height: 145px;
  padding: 10px;
  background-color: ${Colors.white};
  border-color: ${Colors.mediumGrey};
  border-width: 1px;
  border-radius: 4px;
`;
