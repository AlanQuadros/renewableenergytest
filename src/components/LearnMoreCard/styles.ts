import styled from 'styled-components/native';
import {Colors} from '../../resources';

export const LearnMoreContainerPress = styled.TouchableOpacity`
  background-color: ${Colors.purple};
  padding: 20px;
  border-radius: 10px;
`;
